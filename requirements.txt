# requirements.txt - Fixed for Railway deployment
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
requests==2.31.0
pandas==2.1.3
numpy==1.25.2
aiohttp==3.9.0

# Remove problematic dependencies for now
# selenium==4.15.2  # Causes build issues on Railway
# beautifulsoup4==4.12.2  # Can add back later

# Add missing build dependencies
setuptools==69.0.0
wheel==0.42.0
